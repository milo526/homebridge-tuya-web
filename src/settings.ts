/**
 * This is the name of the platform that users will use to register the plugin in the Homebridge config.json
 */
export const PLATFORM_NAME = 'TuyaWebPlatform';

/**
 * This must match the name of your plugin as defined the package.json
 */
export const PLUGIN_NAME = '@milo526/homebridge-tuya-web';

/**
 * Device cache timeout in seconds.
 * Used to determine how long cached device state is considered valid.
 */
export const TUYA_DEVICE_TIMEOUT = 60;
